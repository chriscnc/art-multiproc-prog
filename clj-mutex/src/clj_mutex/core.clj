(ns clj-mutex.core
  (:gen-class))

; turn this into 4 entry point modules.

; UnsafeApp
; LockOneApp
; LockTwoApp
; PetersonApp


(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
